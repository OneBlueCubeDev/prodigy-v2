---
phase: 01-youth-registration
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/app/(app)/youth/page.tsx
  - src/app/(app)/youth/loading.tsx
  - src/components/youth/youth-search-bar.tsx
  - src/components/youth/youth-list-table.tsx
  - src/components/youth/youth-pagination.tsx
autonomous: true
requirements:
  - YOUTH-04

must_haves:
  truths:
    - "Staff can search by name, DOB, or SSN last 4 from a single search bar"
    - "Default view shows most recently registered youth sorted by created_at desc"
    - "Search results update live as staff types (debounced 300ms)"
    - "Pagination uses numbered pages with Previous/Next, stored in URL params"
    - "Empty state shows appropriate message for no-search vs no-results"
  artifacts:
    - path: "src/app/(app)/youth/page.tsx"
      provides: "Youth list Server Component with search and pagination"
      min_lines: 30
    - path: "src/components/youth/youth-search-bar.tsx"
      provides: "Debounced search input updating URL params"
      min_lines: 25
    - path: "src/components/youth/youth-list-table.tsx"
      provides: "Table rendering youth results with Name, DOB, SSN masked, Registered columns"
      min_lines: 40
    - path: "src/components/youth/youth-pagination.tsx"
      provides: "Numbered page pagination with Previous/Next"
      min_lines: 30
  key_links:
    - from: "src/app/(app)/youth/page.tsx"
      to: "src/lib/db.ts"
      via: "db.youth.findMany and db.youth.count queries"
      pattern: "db\\.youth\\.(findMany|count)"
    - from: "src/components/youth/youth-search-bar.tsx"
      to: "next/navigation"
      via: "useRouter and useSearchParams for URL param updates"
      pattern: "useSearchParams|router\\.push"
---

<objective>
Build the youth list page with search bar, results table, and pagination -- all driven by URL params for bookmarkable state.

Purpose: Enable staff to find existing youth records (YOUTH-04) with live-filtering search and paginated results.
Output: Working `/youth` page with search, table, pagination, and loading skeleton.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-youth-registration/01-CONTEXT.md
@.planning/phases/01-youth-registration/01-RESEARCH.md
@.planning/phases/01-youth-registration/01-UI-SPEC.md
@.planning/phases/01-youth-registration/01-01-SUMMARY.md

@src/lib/db.ts
@src/lib/auth.ts
@src/components/ui/table.tsx
@src/components/ui/input.tsx
@src/components/ui/button.tsx
@src/components/ui/skeleton.tsx
@src/app/(app)/layout.tsx

<interfaces>
From prisma/schema.prisma (Youth model):
```prisma
model Youth {
  id                String   @id @default(cuid())
  first_name        String
  last_name         String
  date_of_birth     DateTime @db.Date
  ssn_last4         String?
  created_at        DateTime @default(now())
  @@index([last_name, first_name])
  @@index([ssn_last4])
}
```

From src/lib/auth.ts:
```typescript
export async function requireAuth(): Promise<{ userId: string; role: Role | undefined; siteId: string | undefined }>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build search bar and pagination components</name>
  <files>src/components/youth/youth-search-bar.tsx, src/components/youth/youth-pagination.tsx</files>
  <read_first>
    - src/components/ui/input.tsx (Input component API)
    - src/components/ui/button.tsx (Button variants)
    - .planning/phases/01-youth-registration/01-UI-SPEC.md (search and pagination specs)
    - .planning/phases/01-youth-registration/01-CONTEXT.md (D-07, D-08, D-09 decisions)
  </read_first>
  <action>
**Create `src/components/youth/youth-search-bar.tsx`** as `'use client'` component.

Props: `{ defaultValue?: string }`

Implementation:
- Import `useRouter`, `useSearchParams`, `usePathname` from `next/navigation`.
- Local `useState` initialized from `defaultValue` prop.
- `useEffect` with 300ms debounce: on input change, update URL by calling `router.push(pathname + '?' + params.toString())` where params has `q` set to the input value (delete `q` if empty) and `page` reset to "1".
- Render shadcn `Input` with:
  - `placeholder="Search by name, date of birth, or SSN last 4..."` (per UI-SPEC)
  - `aria-label="Search youth by name, date of birth, or SSN last 4"` (per UI-SPEC accessibility)
  - `SearchIcon` from lucide-react as a decorative icon inside a relative wrapper (position absolute left, input has `pl-10`)
  - `className="h-11"` (44px touch target per UI-SPEC)

**Create `src/components/youth/youth-pagination.tsx`** as `'use client'` component.

Props: `{ currentPage: number; totalPages: number }`

Implementation:
- Import `useRouter`, `useSearchParams`, `usePathname` from `next/navigation`.
- Show pagination only when `totalPages > 1`.
- Render: Previous button (disabled on page 1) | numbered page buttons | Next button (disabled on last page).
- Active page: `className="bg-primary text-primary-foreground"`. Other pages: ghost variant.
- `aria-label="Pagination"` on the `<nav>` wrapper.
- Active page button: `aria-current="page"`.
- On click: update `?page=N` in URL params via `router.push`.
- Show max 5 page numbers around current page (with ellipsis if needed).
  </action>
  <verify>
    <automated>cd /Users/shaneburke/dev/projects/Prodigy-Migration && ls src/components/youth/youth-search-bar.tsx src/components/youth/youth-pagination.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/youth/youth-search-bar.tsx contains "'use client'"
    - src/components/youth/youth-search-bar.tsx contains "useSearchParams"
    - src/components/youth/youth-search-bar.tsx contains "useRouter"
    - src/components/youth/youth-search-bar.tsx contains "setTimeout" (300ms debounce)
    - src/components/youth/youth-search-bar.tsx contains 'aria-label="Search youth by name, date of birth, or SSN last 4"'
    - src/components/youth/youth-search-bar.tsx contains "SearchIcon"
    - src/components/youth/youth-pagination.tsx contains "'use client'"
    - src/components/youth/youth-pagination.tsx contains "useSearchParams"
    - src/components/youth/youth-pagination.tsx contains 'aria-current="page"'
    - src/components/youth/youth-pagination.tsx contains 'aria-label="Pagination"'
    - src/components/youth/youth-pagination.tsx contains "bg-primary"
    - src/components/youth/youth-pagination.tsx contains "Previous"
    - src/components/youth/youth-pagination.tsx contains "Next"
  </acceptance_criteria>
  <done>Search bar debounces at 300ms and updates URL params. Pagination renders numbered pages with Previous/Next and active page indicator.</done>
</task>

<task type="auto">
  <name>Task 2: Build youth list table, loading skeleton, and page route</name>
  <files>src/components/youth/youth-list-table.tsx, src/app/(app)/youth/page.tsx, src/app/(app)/youth/loading.tsx</files>
  <read_first>
    - src/components/ui/table.tsx (Table, TableHeader, TableRow, TableHead, TableBody, TableCell API)
    - src/components/ui/skeleton.tsx (Skeleton component)
    - src/components/ui/button.tsx (Button for "Register Youth" link)
    - src/lib/db.ts (Prisma client import)
    - src/lib/auth.ts (requireAuth)
    - src/components/youth/youth-search-bar.tsx (just created)
    - src/components/youth/youth-pagination.tsx (just created)
    - .planning/phases/01-youth-registration/01-UI-SPEC.md (list page layout, empty states, columns)
  </read_first>
  <action>
**Create `src/components/youth/youth-list-table.tsx`** -- can be a regular component (not 'use client' unless needed for interactivity; receives data as props).

Props:
```typescript
interface YouthListTableProps {
  youth: Array<{
    id: string;
    first_name: string;
    last_name: string;
    date_of_birth: Date;
    ssn_last4: string | null;
    created_at: Date;
  }>;
  total: number;
  page: number;
  pageSize: number;
  query?: string;
}
```

Implementation:
- Use shadcn `Table` with columns: **Name** (first + last), **Date of Birth** (formatted MM/DD/YYYY via `Intl.DateTimeFormat('en-US', { timeZone: 'UTC', month: '2-digit', day: '2-digit', year: 'numeric' })`), **SSN** (show `***-**-{last4}` or "N/A"), **Registered** (relative or formatted date).
- Each row is a `Link` to `/youth/{id}` or wraps the name cell in a link. Use `hover:bg-muted/50` on `TableRow`.
- **Empty state (no search active, no youth):** centered div with: heading "No youth registered yet", body "Get started by registering the first youth in the system.", Button as Link to `/youth/new` with text "Register Youth".
- **Empty state (search active, no results):** centered div with: heading `No results for "[query]"`, body "Try a different name, date of birth, or SSN last 4."
- Render `YouthPagination` below the table with `currentPage={page}` and `totalPages={Math.ceil(total / pageSize)}`.

**Create `src/app/(app)/youth/page.tsx`** as an async Server Component.

Implementation:
- Import `requireAuth` from `@/lib/auth` and call it first.
- Import `db` from `@/lib/db`.
- Read `searchParams` (Next.js 15: `searchParams: Promise<{ q?: string; page?: string }>`). Must `await searchParams`.
- Extract `q` and `page` from searchParams. `currentPage = Math.max(1, parseInt(page ?? '1', 10))`.
- `PAGE_SIZE = 20`.
- Build `where` clause: if `q` exists, use `OR` with `first_name contains insensitive`, `last_name contains insensitive`, `ssn_last4 equals`. If no `q`, empty where (D-08: default shows all, sorted by created_at desc).
- `Promise.all([db.youth.findMany({ where, skip, take, orderBy: { created_at: 'desc' } }), db.youth.count({ where })])`.
- Render page layout:
  - Page wrapper: `max-w-4xl mx-auto px-4 py-8 md:px-6`
  - Top row: `<div className="flex items-center justify-between mb-6">` with `<h1 className="text-2xl font-semibold">Youth Records</h1>` and Button as Link to `/youth/new` with text "Register Youth" and `PlusIcon`.
  - `<YouthSearchBar defaultValue={q} />` with `mb-6`.
  - `<YouthListTable youth={youth} total={total} page={currentPage} pageSize={PAGE_SIZE} query={q} />`.

**Create `src/app/(app)/youth/loading.tsx`**:
- Render 5 skeleton rows matching the table structure using shadcn `Skeleton` component.
- Include skeleton for page header and search bar.
  </action>
  <verify>
    <automated>cd /Users/shaneburke/dev/projects/Prodigy-Migration && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - src/app/(app)/youth/page.tsx contains "async function" (Server Component)
    - src/app/(app)/youth/page.tsx contains "await searchParams"
    - src/app/(app)/youth/page.tsx contains "db.youth.findMany"
    - src/app/(app)/youth/page.tsx contains "db.youth.count"
    - src/app/(app)/youth/page.tsx contains "YouthSearchBar"
    - src/app/(app)/youth/page.tsx contains "YouthListTable"
    - src/app/(app)/youth/page.tsx contains "Youth Records"
    - src/app/(app)/youth/page.tsx contains "Register Youth"
    - src/app/(app)/youth/page.tsx contains "requireAuth"
    - src/components/youth/youth-list-table.tsx contains "Table"
    - src/components/youth/youth-list-table.tsx contains "No youth registered yet"
    - src/components/youth/youth-list-table.tsx contains "No results for"
    - src/components/youth/youth-list-table.tsx contains "***-**-"
    - src/components/youth/youth-list-table.tsx contains "YouthPagination" or "youth-pagination"
    - src/components/youth/youth-list-table.tsx contains "Intl.DateTimeFormat"
    - src/app/(app)/youth/loading.tsx contains "Skeleton"
    - TypeScript compilation produces no errors related to youth files
  </acceptance_criteria>
  <done>Youth list page renders at /youth with server-side search, paginated results table, loading skeleton, empty states, and "Register Youth" button linking to /youth/new.</done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Dev server loads `/youth` without crash
- Default view shows youth ordered by created_at desc
- Search input updates URL params with debounce
- Pagination visible when results exceed 20
- Empty states render correctly
</verification>

<success_criteria>
- /youth renders youth list with search and pagination
- URL params ?q and ?page are bookmarkable
- Search debounces at 300ms, updates results live
- Default view sorted by created_at desc (D-08)
- Empty states match UI-SPEC copywriting contract
- Pagination shows Previous / numbered / Next (D-09)
</success_criteria>

<output>
After completion, create `.planning/phases/01-youth-registration/01-03-SUMMARY.md`
</output>

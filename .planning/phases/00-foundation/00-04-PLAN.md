---
phase: 00-foundation
plan: 04
type: execute
wave: 3
depends_on: ["00-02", "00-03"]
files_modified: []
autonomous: false
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - INFRA-05

must_haves:
  truths:
    - "Dev server starts and renders pages"
    - "Sign-in redirects to Clerk"
    - "Program selector shows program cards"
    - "App shell sidebar and header render correctly"
    - "Health check returns 200"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete Phase 0 foundation works end-to-end: dev server starts, auth flow works, UI renders correctly, and all tests pass.

Purpose: Human verification that the scaffolded project, auth integration, UI shell, and test infrastructure all function together before moving to Phase 1.
Output: Confirmed working foundation ready for Phase 1.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/00-foundation/00-UI-SPEC.md
@.planning/phases/00-foundation/00-01-SUMMARY.md
@.planning/phases/00-foundation/00-02-SUMMARY.md
@.planning/phases/00-foundation/00-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix any failures</name>
  <files></files>
  <read_first>
    vitest.config.ts
    package.json
  </read_first>
  <action>
    1. Run the full Vitest suite:
       ```bash
       pnpm vitest run
       ```

    2. If any tests fail, read the failing test file and the source file it tests, diagnose the issue, and fix it.

    3. Run TypeScript type checking:
       ```bash
       pnpm tsc --noEmit
       ```

    4. If type errors exist, fix them. Common issues:
       - Missing type imports
       - Zod v4 API differences (z.string() vs z.coerce.string())
       - Clerk type augmentation in globals.d.ts

    5. Verify all tests pass and types check before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>pnpm vitest run 2>&1 | tail -5 && pnpm tsc --noEmit 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm vitest run` exits 0 with all tests passing
    - `pnpm tsc --noEmit` exits 0 with no type errors
  </acceptance_criteria>
  <done>Full test suite passes, TypeScript compiles without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of complete Phase 0 foundation</name>
  <files></files>
  <action>
    Present the verification checklist to the user. The user will manually verify the running application against the Phase 0 requirements and UI-SPEC contract.
  </action>
  <what-built>
    Complete Phase 0 foundation:
    - Next.js 16 project with Clerk auth, Prisma schema, and all infrastructure utilities
    - App shell with sidebar (240px) and header (56px)
    - Program selector page with card grid
    - Clerk sign-in integration
    - Health check endpoint
    - Full test suite (SSN encryption, grant year, auth, audit, health check)
    - Seed script for lookup tables and test programs
  </what-built>
  <how-to-verify>
    **Pre-requisites (one-time setup):**
    1. Start a local PostgreSQL: `docker run --name prodigy-pg -e POSTGRES_PASSWORD=dev -p 5432:5432 -d postgres:15`
    2. Create the database: `docker exec prodigy-pg psql -U postgres -c "CREATE DATABASE prodigy;"`
    3. Set up Clerk (if not done): Create a Clerk dev app at clerk.com, get API keys
    4. Generate SSN key: `node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"`
    5. Update `.env.local` with real values for DATABASE_URL, CLERK keys, and SSN key
    6. Run migrations: `pnpm prisma db push`
    7. Seed data: `pnpm prisma db seed`
    8. In Clerk Dashboard: Sessions > Customize session token > Add: `{ "metadata": "{{user.public_metadata}}" }`
    9. In Clerk Dashboard: Multi-factor > Enable MFA enforcement (AUTH-01)

    **Verification steps:**
    1. Start dev server: `pnpm dev`
    2. Navigate to http://localhost:3000 — should redirect to /select-program, then to Clerk sign-in
    3. Sign in with Clerk — should see program selector with 3 program cards
    4. Verify page heading says "Select a Program" and subheading says "Choose the program you're working in today."
    5. Click "Continue to Dashboard" on a program card — should navigate to /dashboard with app shell
    6. Verify sidebar is 240px wide with "Prodigy" title and nav items
    7. Verify header is 56px tall with role badge
    8. On mobile viewport (< 768px): sidebar should be hidden, hamburger menu visible
    9. Navigate to http://localhost:3000/api/health — should return `{ "status": "ok" }`
    10. Run `pnpm vitest run` — all tests should pass
  </how-to-verify>
  <verify>
    <automated>curl -s http://localhost:3000/api/health 2>/dev/null || echo "Dev server not running — manual verification required"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms sign-in redirects to Clerk
    - User confirms program selector displays 3 program cards
    - User confirms "Select a Program" heading matches UI-SPEC copy
    - User confirms app shell sidebar is ~240px and header is ~56px
    - User confirms mobile viewport hides sidebar
    - /api/health returns { "status": "ok" }
  </acceptance_criteria>
  <done>User has approved the complete Phase 0 foundation — all screens, auth flow, and infrastructure verified.</done>
  <resume-signal>Type "approved" to complete Phase 0, or describe issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. All Vitest tests pass
2. TypeScript compiles without errors
3. Dev server starts and serves pages
4. Auth flow works end-to-end
5. UI matches UI-SPEC contract
</verification>

<success_criteria>
- `pnpm vitest run` exits 0
- `pnpm tsc --noEmit` exits 0
- Dev server starts on port 3000
- /sign-in renders Clerk sign-in
- /select-program shows program cards after login
- /dashboard renders with app shell (sidebar + header)
- /api/health returns 200 { status: ok }
- Human approves visual and functional verification
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-04-SUMMARY.md`
</output>

import { YouthRegistrationForm } from '@/components/youth/youth-registration-form';

/**
 * /youth/new — New youth registration page.
 * Renders the registration form which handles all form state client-side.
 */
export default function NewYouthPage() {
  return <YouthRegistrationForm />;
}
